version: 0.1
component: build
timeoutInSeconds: 1000
shell: bash

steps:
  - type: Command
    name: "Install project.."
    command: |
      mvn versions:set -DnewVersion= ${OCI_PIPELINE_ID}
      mvn clean install